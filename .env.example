# Django core
SECRET_KEY=your-production-secret
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (Railway te da esta variable automáticamente)
DATABASE_URL=postgresql://postgres:<password>@<host>:5432/lynkledger

# Redis (Railway también te da esta automáticamente si usas su Redis addon)
REDIS_URL=redis://default:<password>@<host>:6379

# OpenAI API (si usas AI en tu sistema)
OPENAI_API_KEY=sk-...

# Email (configurado para producción más adelante)
DEFAULT_FROM_EMAIL=noreply@lynkledger.com
